from setuptools import setup, find_packages

setup(
    name="emotion_classification_dlfa",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # Add your project's dependencies here
        # e.g., 'numpy>=1.19.0',
    ],
)
